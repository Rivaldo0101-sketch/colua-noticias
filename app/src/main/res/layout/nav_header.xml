<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:background="@color/white">

    <!-- Botón Perfil a la izquierda -->
    <ImageView
        android:id="@+id/btn_profile"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:contentDescription="Mi Perfil"
        android:padding="6dp"
        android:src="@drawable/ic_person"
        app:tint="@color/colua_navy" />

    <!-- Centro: Encabezado con COLUA en negrita + distintivo.png -->
    <LinearLayout
        android:id="@+id/layout_brand_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_header_bold_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins_regular"
            android:text="COLUA"
            android:textColor="@color/colua_navy"
            android:textSize="22sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/nav_header_distintivo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:adjustViewBounds="true"
            android:src="@drawable/distintivo_colua" />

    </LinearLayout>

    <!-- Botón X para cerrar el menú lateral usando cerrar.png -->
    <ImageView
        android:id="@+id/close_drawer"
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:contentDescription="Cerrar menú"
        android:padding="6dp"
        android:src="@drawable/cerrar"
        app:tint="@color/text_grey" />

</RelativeLayout>
